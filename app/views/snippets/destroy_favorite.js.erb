$('#<%= @favorite.id %>').fadeOut('fast', function () {
	$(this).parent().appendTo('<%= button_to "Favorite", favorite, :remote => true, class: "btn btn-default btn-sm col-xs-1 unfavorited" %>');
	$(this).remove();
});
